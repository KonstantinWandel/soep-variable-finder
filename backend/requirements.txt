# Runtime dependencies for the GeoLAB metadata finders (CPU inference).
# Pins reflect the verified deployment environment.
#
# Install CPU-only torch first (avoids pulling the large CUDA build), then the rest:
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
duckdb==1.5.3
numpy==1.26.4
pandas==3.0.3
sentence-transformers==5.5.1
faiss-cpu==1.14.2
sentencepiece==0.2.1
protobuf==7.35.0
transformers==5.10.2
